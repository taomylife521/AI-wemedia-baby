# 媒小宝 - 依赖包列表
# Python 3.12 (推荐) 或 3.10+

# ============================================
# 核心依赖 (必需)
# ============================================

# 界面框架
# 注意：PySide6 6.x 版本已包含 Charts 模块，无需单独安装
PySide6>=6.4.0

# UI组件库（Fluent Design）
# 注意：必须使用 PySide6-Fluent-Widgets（专门为 PySide6 设计）
# 不要使用 PyQt-Fluent-Widgets，它与 PySide6 6.10+ 不兼容
PySide6-Fluent-Widgets>=1.5.0

# Qt 与 asyncio 事件循环集成
qasync>=0.27.0

# 加密和安全
cryptography>=41.0.0
bcrypt>=4.0.0
keyring>=24.0.0

# 异步 I/O
aiohttp>=3.9.0
aiosqlite>=0.19.0
aiofiles>=23.0.0
aiocache>=0.12.0

# ORM 框架与数据库迁移
tortoise-orm>=0.21.0
aerich>=0.7.0

# 数据验证
pydantic>=2.0.0

# 任务调度
APScheduler>=3.10.0

# 浏览器自动化
playwright>=1.40.0
undetected-playwright>=0.3.0

# 工具库
PyYAML>=6.0.0
psutil>=5.9.0
python-dateutil>=2.8.0

# 视频处理
ffmpeg-python>=0.2.0

# ============================================
# 可选依赖 (按需安装)
# ============================================

# 重试机制 (如需高级重试策略)
tenacity>=8.0.0

# 熔断器 (如需故障隔离)
pybreaker>=1.0.0

# 文件监控 (如需配置热更新)
watchdog>=3.0.0

# ============================================
# 开发/构建工具 (开发环境安装)
# ============================================

# 测试框架
pytest>=7.4.3
pytest-asyncio>=0.21.1
pytest-cov>=4.1.0
pytest-mock>=3.12.0

# 打包工具 (仅打包时需要)
# Nuitka>=2.0.0

# 代码格式化
# black>=23.0.0

# 代码检查
# pylint>=3.0.0
# mypy>=1.0.0
